{
  "name": "ip-icecube",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "node-sass --output-style compressed style.scss style.css",
    "develop": "npm run browsersync & node-sass -w style.scss style.css",
    "browsersync": "browser-sync start --server --files 'style.scss'"
  },
  "author": "Irko Palenius",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.11.1",
    "node-sass": "^3.7.0"
  }
}
